# Exp Index

Use this file as the compact index of Kotlin and Android experience notes.

Format for each entry:

- `note-name` — short topic — relates to `android-stack` / `kotlin-core` / `both` / `note only`

Add new entries at the end.
